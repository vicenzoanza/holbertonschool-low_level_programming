#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	char l;

	for (i = '0'; i <= '9'; i++)
	putchar(i);
	for (l = 'a'; l < 'g'; l++)
	putchar('\n');
	return (0);

}
