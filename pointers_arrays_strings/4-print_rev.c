#include "main.h"
#include <stdio.h>
/**
* print_rev - prints a string, in reverse, followed by new line.
* @s: string
**/
void print_rev(char *s)
{
char *s[95]

printf("\nI do not fear computers. I fear the lack of them - Isaac Asimov");
scanf("%s", *s);
printf("\n I do not fear computers. I fear the lack of them - Isaac Asimov:%s", strrev(*s));
return (0);
}
