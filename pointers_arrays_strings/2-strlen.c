#include "main.h"
#include "stdio.h"
#include "string.h"
/**
 * _strlen - function that returns the length of a string.
 * @s: string
 **/
int _strlen(char *s)
{
char c[16]="My first strlen";

printf("%s\n", c);
}
