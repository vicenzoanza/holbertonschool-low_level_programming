#include "main.h"
/**
* print_numbers - prints the numbers 0 to 9, followed by a new line.
**/
void print_numbers(void)
{
int n;

for (n > 0 ; n <= 9; n++)
printf("%d", n);
_putchar('\n');
return (0);
}
