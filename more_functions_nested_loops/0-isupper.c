#include <main.h>
#include <stdio.h>
#include <ctype.h>
/**
 * main - check the code.
 *
 * Return: Always 0.
 **/
int main(void)
{
	char c;
	int _isupper(int c);

if	 c = 'A'
	 printf("%c: %d\n", c, _isupper(c));
return (1);
else 	c = 'a';
	printf("%c: %d\n", c, _isupper(c));
	return (0);
}
