#include <stdio.h>
/**
 * main - Prints its name, followed by a new line.
 * @argc: argument count.
 * @argv: argument vector.
 * Return: Always.
 **/
int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
(void)argc;
return (0);
}
